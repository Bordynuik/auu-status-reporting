services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATA_DIR: /usr/src/app/data
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
